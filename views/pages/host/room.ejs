<!DOCTYPE html>
<html>
<head>
  <% include ../../partials/header.ejs %>
</head>

<body>


    <% include ../../partials/roomHeader.ejs %>

    <main class="ui container" v-cloak>

        <div class="ui stackable basic segment grid">

            <div class="two wide column">
                <div id="qrcode"></div>
              </div>

            <div class="fourteen wide column">

                <h1 class="ui header">

                    <div class="content">
                        {{ room.title }}
                        <div class="sub header userName">{{ user.nickname }}'s room</div>
                    </div>

                </h1>

                <div class="ui mini horizontal statistic">
                    <div class="value">
                      <i class="users icon"></i> {{ participants }}
                    </div>
                    <div class="label">
                      Participants
                    </div>
                </div>

            </div>

              <div id="whiteboardModal" class="ui modal">

                  <i class="close icon"></i>

                  <div class="content">

                      <div class="ui huge left floated vertical buttons">

                          <div class="ui left pointing compact dropdown icon button paintbrushMenu">
                              <i class="paint brush icon"></i>
                                <div class="menu">
                                    <div class="item" v-on:click="setPaintbrushSize()"><i class="tiny circle icon"></i></div>
                                    <div class="item" v-on:click="setPaintbrushSize('8')"><i class="circle icon"></i></div>
                                    <div class="item" v-on:click="setPaintbrushSize('16')"><i class="big circle icon"></i></div>
                                </div>
                          </div>

                          <div class="ui icon button clearWhiteboard"><i class="eraser icon"></i></div>

                      </div>

                      <div id="whiteboard">

                          <canvas width="700" height="300"></canvas>

                      </div>

                  </div>

              </div>

            <div class="ten wide basic clearing segment column">

                    <div class="ui padded top attached segment">

                        <div class="ui transparent fluid huge left icon input">
                          <i class="write icon"></i>
                          <input type="text" v-model="activityTitle" placeholder="Choose a title" required="true">
                        </div>

                    </div>

                    <div class="ui padded bottom attached segment">

                        <div class="ui top attached label">Answers</div>

                        <div class="ui large fluid action input">
                            <input id="newAnswerText" type="text" v-model="newAnswerText" placeholder="Enter an answer" v-on:keyup.enter="addNewAnswer">
                            <a href="#" class="ui button" v-on:click="addNewAnswer"><i class="plus icon"></i> Add</a>
                        </div>

                        <div class="ui segments" v-if="answers.length > 0">

                            <div class="ui transparent fluid labeled action input segment" v-for="(answer, index) in answers">
                                <input type="text" v-model="answer.title">
                                <a href="#" v-on:click="vote(index, $event)" class="ui icon button" data-tooltip="Vote" data-inverted><i class="bar chart icon"></i></a>
                                <a href="#" v-on:click="removeAnswer(index, $event)" class="ui icon button" data-tooltip="Remove this answer" data-inverted><i class="remove icon"></i></a>
                            </div>

                        </div>

                    </div>

                    <a id="startActivity" href="#" class="ui right floated primary button" v-on:click="logData" :class="{ disabled: answers.length === 0 || activityTitle}"><i class="play icon"></i> Start activity</a>

            </div>

            <div class="six wide column">

                <h3 class="ui dividing header">Preview:</h3>

                <div id="answerChart" style="height: 300px; width: 100%;"></div>

                <h3 class="ui dividing header">Preview:</h3>

                <div id="openResponses">

                    <transition-group name="list-complete" tag="p">

                        <span v-for="(answer, index) in answers" v-bind:key="answer" class="list-answers-item">

                            <div class="ui big left pointing label list-answers-item" v-if="index % 2 == 0" :key="index">
                              {{ answer.title }}
                            </div>
                            <div class="ui big right pointing label list-answers-item" v-if="index % 2 != 0" :key="index">
                                {{ answer.title }}
                            </div>

                        </span>

                      </transition-group>

                </div>

            </div>

        </div>

    </main>

    <% include ../../partials/roomScripts.ejs %>
    <script>

        var user = <%-user%>;
        var room = <%-room%>;
        $('#whiteboardModal').modal();
        $('.paintbrushMenu').dropdown();
    </script>
    <script type="text/javascript" src="/js/session.js"></script>
    <script type="text/javascript" src="/js/client.js"></script>

</body>
</html>
